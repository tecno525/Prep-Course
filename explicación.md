#variable
Una variable es un espacio donde se pueden guardar  datos de un programa, que se usaran   en un futuro, contiene informacion conocida o desconocida .ademas puede ser utilizada para un proceso repetitivo.

#strings
es una secuencia de caracteres usado para representar el texto, podemos usarla para almacenar nombres, descripciones, textos que aparezcan en la interfaz gráfica.Una cadena puede contener de 0 a aproximadamente dos mil millones de  caracteres Unicode
                                                                              
#numbers
  Los números son simplemente, números. Los números NO se escriben entre comillas. Pueden ser negativos también. tiene una limitación en el tamaño de un número . objeto primitivo envolvente que permite representar y manipular valores numéricos .

#booleanos
puede representar valores de lógica binaria, esto es 2 valores, que normalmente representan falso o verdadero.​ Se utiliza normalmente en la programación, estadística, electrónica, matemáticas                                      